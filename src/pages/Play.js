import React, { useEffect } from 'react';

function Play({ gameId }) {


    return (
        <div>
            <h1>Game Id: {gameId}</h1>
            {/* Otro contenido de la página Play */}
        </div>
    );
}

export default Play;